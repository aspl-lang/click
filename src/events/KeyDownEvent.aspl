import graphics

[public]
class KeyDownEvent extends KeyEvent {

    [public]
    method construct(KeyCode key){ // TODO: This override is only needed because of a compiler bug
        parent(KeyEvent).construct(key)
    }

}