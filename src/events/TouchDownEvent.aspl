[public]
class TouchDownEvent extends TouchEvent {

    [public]
    method construct(TouchPointer pointer){ // TODO: This override is only needed because of a compiler bug
        parent(TouchEvent).construct(pointer)
    }

}