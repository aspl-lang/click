import graphics
import math.geometry

[public]
class MouseUpEvent extends MouseEvent {

    [readpublic]
    property MouseButton button

    [public]
    method construct(Point position, MouseButton button){
        parent(MouseEvent).construct(position)
        this.button = button
    }

}