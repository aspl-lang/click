[public]
class UnifiedClickEvent extends UnifiedPointerEvent {

    [public]
    method construct(UnifiedPointer pointer){ // TODO: This override is only needed because of a compiler bug
        parent(UnifiedPointerEvent).construct(pointer)
    }

}